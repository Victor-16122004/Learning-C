#include <stdio.h>

int main(){
    int a;
    char ch = 'a';
    ch = 'c';
    ch = '\n';// represents a new line (it is single letter so we are calling this types of letter a escape sequence character)
    printf("Hey I am  good \" nice");
    return 0;
}