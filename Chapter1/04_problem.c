#include <stdio.h>

int main() {
    float p=34.1;
    int r=8;
    int t=5;
    //printf("The simple interest for principal amount %f, rate of interest %d and time %d is %f\n", p, r, t, (p * r * t) / 100.0);
    printf("the value of simple intrest is %f\n", (p * r * t) / 100.0);
    return 0;
}